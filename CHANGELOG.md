# 更新记录

## 3.9.5

- 安装完成后启动改为通过系统外壳打开，并在托盘就绪后反复注册图标，避免安装向导仍打开时托盘空白。
- 设置窗口在显示前先按屏幕算好尺寸，音频设备窗口启动时不出现在屏幕上，减少启动时一闪而过的小窗口。
- 登录注销和安装升级时会真正退出，便于替换文件；再次打开会唤起已运行的实例。
- 安装包补充发布者信息。未代码签名时 Windows 仍可能显示安全提示，可选择“更多信息”后继续运行。

## 3.9.4

- 设置改动立即保存，去掉“保存设置”按钮；拼音层和全局快捷键仍点击“应用”。“开始输入”也会写入当前设置。
- “启动与快捷键”移到设置顶部。新增“启动后默认收进系统托盘”，平时打开不弹出设置窗口；登录自启仍收进托盘。
- 配置写入失败仍保留原文件并提示，再改一项设置即可重试。

## 3.9.3

- 修复通过摩斯码按住鼠标后，暂停、返回设置或退出仍残留拖动状态的问题；只释放程序记录的鼠标按钮，异常清理继续尝试其他按钮。
- 配置改为先完整写入临时文件再原子替换；保存失败保留原文件，并在设置页或托盘显示提示。读取异常字段时单独回退默认值，保留其他有效偏好，不因读取异常覆盖原文件。
- 减少空闲时重复的界面反馈和音频静音计算，保留首次及重置反馈、点划时序、确认进度和声音边沿。
- 修正自动适应的说明：只有英文完整码表可关闭，拼音和精简模式始终适应窗口；明确源码首次运行会沿用仓库已有的三键、静音配置。
- 清理未参与当前运行和构建的旧预测数据库、训练资源、WAV 音效、开发试验脚本及 Word 码表，保留图标、标识图片和历史发布说明；个人配置不受影响。

## 3.9.2

- 将原“单击切换”改为“双击切换”：单击 F22 显示／隐藏码表，双击只切换一次中英文层，并保持码表原有显隐状态；操作按键仍可修改。
- 单击最多等待 300 毫秒以区分双击；等待期间开始输入点划会立即确认显隐，点划仍按当前层输入。双击切换模式中长按不触发操作。
- 新用户默认“双击切换”，旧“单击切换”配置沿用新手势；保留“按住使用”及其已有设置，继续按住拼音、松开英文、双击显隐。
- 更新中文使用说明与生成码表中的手势说明；微软拼音同步、未选字时保留原拼音及各项编码保持原有行为。

## 3.9.1

- 修复微软拼音尚未选字时，使用 F22 单击切回英文或松开按住键无法完成同步的问题；未选字的内容保留为原拼音字母，不自动选择中文候选。
- 切英文改用原生输入法关闭接口结束组词，不额外发送 Enter、Space 或 Shift；仍在切换后读取状态确认结果。
- 改进同步失败提示，区分输入窗口变化、访问权限不足、输入法响应超时及切换未确认，并记录诊断信息。
- 更新中文使用与开发说明，说明未选字时切回英文的行为。

## 3.9.0

- 新增可选的微软拼音中／英文状态双向同步，默认关闭：外部切换中英时码表跟随，软件切层时定向切换当前输入窗口，并读取状态确认结果。
- 设置增加独立的“拼音与码表切换”区域，直接显示“按住使用”和“单击切换”两个选项，修改后点击“应用”。
- 修复快速双击显隐时码表短暂切入两次拼音层的问题；按住约 180 毫秒或开始输入点划后才确认长按，双击不会反复切换同步的微软拼音。
- 松开时尚未确认的电码先在原输入法模式完成，再退回英文；不自动按 Enter、Space 或提交中文候选。
- 拼音层补齐 26 个符号，19 个沿用英文码，7 个冲突符号使用扩展码；通过键盘输入，标点样式由当前输入法决定。
- 同步请求绑定发起时的输入窗口，切换目标后丢弃旧请求；输入法身份不明确、访问受限或状态不可读时明确提示，不把未知状态当作英文。
- 设置窗口根据屏幕可用区域确定初始大小，窄屏选项换行、内容仅纵向滚动，底部按钮保持可见；换屏和显示缩放调整保留正常手动窗口大小。
- 更新中文使用说明、拼音码表及原生输入法验证方法。

## 3.8.0

- 新增拼音输入层：沿用 37 个注音电码，键帽显示拼音，声母在左、韵母在右，并补充完整复合韵母及 y / w 拼写字母。
- 拼音层按键默认 F22，可配置其他独立功能键；支持默认的“按住使用”和可选的“单击切换”，两种方式均用双击显示／隐藏码表。
- 保留额外的显示／隐藏快捷键；与拼音层按键相同时统一使用手势，避免重复触发。当前电码固定使用开始输入时所属的层。
- 拼音通过拉丁按键送入系统拼音输入法选字；按输入法约定将 ü、üe、ê 分别输出为 v、ve、e，不引入自带中文候选系统。
- 为拼音层的冲突编辑键分配独立编码，保留数字及鼠标动作；英文层原有码保持不变。
- 增加自动生成的[拼音码表](docs/pinyin-code-chart.md)，同一检查工具验证英文、拼音两份文档，并更新中文使用说明。
